<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="MedSestriManipulations.Controls.TopNavigationMenu"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Loaded="OnLoaded">

    <Grid BackgroundColor="{StaticResource WarmSurface}" RowDefinitions="Auto,Auto,Auto">

        <Grid Grid.Row="0" Padding="18,14,18,9">
            <Label
                Text="Медицински Сестри"
                Style="{StaticResource WarmPageTitle}"
                HorizontalOptions="Center"
                VerticalOptions="Center" />
        </Grid>

        <Grid Grid.Row="1"
              Padding="9,0"
              ColumnDefinitions="*,*,*,*"
              ColumnSpacing="4">

            <Grid Grid.Column="0" MinimumHeightRequest="48">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Tapped="GoHome" />
                </Grid.GestureRecognizers>
                <VerticalStackLayout Spacing="4" VerticalOptions="Center">
                    <Label x:Name="HomeTabLabel"
                           Text="Начало"
                           Style="{StaticResource WarmButtonText}"
                           FontSize="14"
                           HorizontalOptions="Center" />
                    <BoxView x:Name="HomeUnderline"
                             HeightRequest="2"
                             WidthRequest="36"
                             Color="{StaticResource WarmAccent}"
                             IsVisible="False"
                             HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Grid>

            <Grid Grid.Column="1" MinimumHeightRequest="48">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Tapped="GoHistory" />
                </Grid.GestureRecognizers>
                <VerticalStackLayout Spacing="4" VerticalOptions="Center">
                    <Label x:Name="HistoryTabLabel"
                           Text="История"
                           Style="{StaticResource WarmButtonText}"
                           FontSize="14"
                           HorizontalOptions="Center" />
                    <BoxView x:Name="HistoryUnderline"
                             HeightRequest="2"
                             WidthRequest="36"
                             Color="{StaticResource WarmAccent}"
                             IsVisible="False"
                             HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Grid>

            <Grid Grid.Column="2" MinimumHeightRequest="48">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Tapped="GoCatheters" />
                </Grid.GestureRecognizers>
                <VerticalStackLayout Spacing="4" VerticalOptions="Center">
                    <Label x:Name="CathetersTabLabel"
                           Text="Катетри"
                           Style="{StaticResource WarmButtonText}"
                           FontSize="14"
                           HorizontalOptions="Center" />
                    <BoxView x:Name="CathetersUnderline"
                             HeightRequest="2"
                             WidthRequest="36"
                             Color="{StaticResource WarmAccent}"
                             IsVisible="False"
                             HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Grid>

            <Grid Grid.Column="3" MinimumHeightRequest="48">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Tapped="GoStatistics" />
                </Grid.GestureRecognizers>
                <VerticalStackLayout Spacing="4" VerticalOptions="Center">
                    <Label x:Name="StatisticsTabLabel"
                           Text="Статистика"
                           Style="{StaticResource WarmButtonText}"
                           FontSize="14"
                           HorizontalOptions="Center" />
                    <BoxView x:Name="StatisticsUnderline"
                             HeightRequest="2"
                             WidthRequest="36"
                             Color="{StaticResource WarmAccent}"
                             IsVisible="False"
                             HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Grid>
        </Grid>

        <BoxView Grid.Row="2" HeightRequest="1" Color="{StaticResource WarmDivider}" />
    </Grid>
</ContentView>
